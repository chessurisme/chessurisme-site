"use strict";

import { createIcons, Play, CircleUserRound, Menu } from 'lucide';

createIcons({
  attrs: {
    stroke: 'var(--stroke-color)'
  },

  icons: {
    // Home Page
    Menu,
    Play,
    CircleUserRound
  }
});